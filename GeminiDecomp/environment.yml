# conda env create -f environment.yml
# conda activate gemini_decomp
name: gemini_decomp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - numpy>=1.26
      - scipy>=1.12
      - matplotlib>=3.8
      - seaborn>=0.13
      - pandas>=2.2
      - tqdm>=4.67
      - jupyter
      - ipykernel
      - google-genai>=1.0
